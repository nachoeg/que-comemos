import { AuthRequestInterceptor } from './auth-request-interceptor';

describe('AuthRequestInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthRequestInterceptor()).toBeTruthy();
  });
});
