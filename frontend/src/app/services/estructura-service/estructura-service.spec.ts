import { EstructuraService } from '../estructura-service';

describe('EstructuraService', () => {
  it('should create an instance', () => {
    expect(new EstructuraService()).toBeTruthy();
  });
});
